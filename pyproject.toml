[project]
name = "trip-recap"
version = "0.1.0"
description = "Metadata-driven trip recap app"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
]

[tool.poetry]
package-mode = false
